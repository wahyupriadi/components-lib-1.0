import { Story, Preview, Props } from "@storybook/addon-docs/blocks";

# Box

Box component based on `div` element. Its role as multipurpose `div` has many props for styling the component.
Box component provides a lot of utility props (Utils Props).

### Spacing Utils Props
All spacing utils props is [here](/?path=/docs/utils-spacing--list)
<Preview>
  <Story id="atom-box--margin" />
  <Story id="atom-box--padding" />
</Preview>

### Color Utils Props
All color utils props is [here](/?path=/docs/utils-color--list)
<Preview>
  <Story id="atom-box--background-color" />
</Preview>

### Border Utils Props
All border utils props is [here](/?path=/docs/utils-border--list)
<Preview>
  <Story id="atom-box--border" />
</Preview>