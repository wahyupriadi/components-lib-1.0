import { Story, Preview } from "@storybook/addon-docs/blocks";

# Flexbox
```js
<Component justifyContent="center" flex="1 1 auto"></Component>
```

**Note**: If `alignItem`, `alignContent`, `justifyContent`, `flexWrap`, or `flexDirection` applied, the component display property will be assign as `display: flex` automatically.
<Story id="utils-flexbox--list" />
